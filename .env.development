# Loaded by Vite during `vite dev` (development mode).
# Points at the locally-running nutshell-mvp backend so the EmailCaptureModal
# doesn't fall through to production. Override per-developer in .env.local
# (git-ignored) if your backend isn't on the default port.
VITE_API_BASE_URL=http://localhost:5004
